# https://docs.github.com/en/actions/learn-github-actions/reusing-workflows

name: Module CI

on:
  push:
  pull_request:

jobs:
  ci:
    uses: emteknetnz/octopus/.github/workflows/ci.yml@main
    with:
      require_extra: 'silverstripe/campaign-admin:1.50.x-dev as 1.9.x-dev'
