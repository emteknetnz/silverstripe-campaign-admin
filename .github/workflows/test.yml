name: Test
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        uses: emteknetnz/gha-tag-release@main
        with:
          sha: 0b33b7c38d793ff64a1649644702af7d49473cd5
          tag: mytag
          delete_existing: true
          release: true
          body: this is my body
          generate_release_notes: false
